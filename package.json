{
  "name": "next-strapi-tailwind-graphql-template",
  "version": "1.0",
  "private": true,
  "workspaces": [
    "site",
    "strapi"
  ],
  "dependencies": {
    "concurrently": "^7.2.0"
  },
  "scripts": {
    "dev": "concurrently --names \"SITE,STRAPI,STORYBOOK\" --prefix \"({name})\" --prefix-colors \"cyan.bold,yellow.bold,green.bold\"  \"yarn workspace site dev\" \"yarn workspace strapi develop\" \"yarn workspace site storybook\"",
    "build": "yarn strapi build",
    "start": "yarn strapi start"
  },
  "resolutions": {
    "react": "17"
  }
}
